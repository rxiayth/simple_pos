1. change functions so that it is only used in immediate child.
    - e.g. selectMenuItem is defined in App.js and used in
        MenuItem. (2 steps down)
    - try to change to... perhaps
        define updateCart in App
        used in Menu

        define selectMenuItem in Menu
        used in MenuItem

    - mebbe...
        updateCart(sku, quantity)
            use in both MenuItem with sku +1
            and mebbe Cart to minus with sku -1

        selectMenuItem(sku) {
            this.props.updateCart(sku, 1);
        }

2. style. convert all of css into style

3. disable the button when maxed

4. borders on menuItem

5. minus button on cartitem

-- ??
6. swipe to remove?

7. generalize funcs in components

8. click settings to update Menu into Setting
    - change menu section into setting section

9. database, change to JSON with set values for price and etc

10. 
